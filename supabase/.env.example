# Supabase Edge Function secrets
# Set these with: supabase secrets set KEY=value
# These are server-side secrets — never shipped to the browser.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
TRIGGER_SECRET_KEY=
TRIGGER_API_URL=
