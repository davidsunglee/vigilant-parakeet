# Supabase public client credentials
# These are safe to expose in the browser (anon key is RLS-protected)
# Set as Vercel project environment variables for production deploys
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
